# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-4.6.2
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2024-02-21T03:49:15Z"
  labels:
    app: 5udm-pod
  name: 5udm-pod
spec:
  containers:
  - image: localhost/nf_separated_container_udm:latest
    name: 5udm
    securityContext:
      privileged: true
    stdin: true
    tty: true

